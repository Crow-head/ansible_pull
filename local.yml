- hosts: localhost
  connections: local
  become: true

  pre_tasks:
          - name: update repo
            apt:
                    update_cache: yes
            change_when: false

  tasks:
          - include: tasks/packages.yml



